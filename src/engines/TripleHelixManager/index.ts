// TripleHelixManager/index.ts
// Export TripleHelixManager and related types

import { TripleHelixManager } from './TripleHelixManager';
import type { 
  TripleHelixManagerInterface, 
  LearningPath,
  PathStatus,
  UserProgress,
  PathSelection,
  PathUpdateResult
} from './TripleHelixTypes';

export { TripleHelixManager };
export type { 
  TripleHelixManagerInterface, 
  LearningPath,
  PathStatus,
  UserProgress,
  PathSelection,
  PathUpdateResult
};